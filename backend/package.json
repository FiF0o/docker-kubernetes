{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "db:start": "docker run --name mongodb --rm -d -p 27017:27017 mongo",
    "db:stop": "docker stop mongodb",
    "docker:build": "docker build -t goals-node .",
    "docker:start": "docker run --name goals-backend --rm -d -p 80:80 goals-node",
    "docker:stop": "docker stop goals-backend"
  },
  "author": "Maximilian Schwarzmüller / Academind GmbH",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.3",
    "morgan": "^1.10.0"
  }
}
